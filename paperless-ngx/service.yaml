apiVersion: v1
kind: Service
metadata:
  name: paperless

spec:
  selector:
    app.kubernetes.io/name: paperless
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: 8000
      protocol: TCP
